apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "apache-chart.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "apache-chart.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "apache-chart.name" . }}
    spec:
      containers:
        - name: apache
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 80
          volumeMounts:
            - name: config-volume
              mountPath: /usr/local/apache2/conf/httpd.conf
              subPath: httpd.conf
            - name: html-volume
              mountPath: /usr/local/apache2/htdocs/index.html
              subPath: index.html
      volumes:
        - name: config-volume
          configMap:
            name: {{ include "apache-chart.fullname" . }}-config
        - name: html-volume
          configMap:
            name: {{ include "apache-chart.fullname" . }}-config