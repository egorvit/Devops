apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "Chart.fullname" . }}-config
data:
  httpd.conf: |
    {{ .Values.apacheConfig.customConfig | indent 4 }}
  index.html: |
    {{ .Values.indexPage | indent 4 }}